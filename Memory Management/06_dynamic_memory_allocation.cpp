#include<bits/stdc++.h>
using namespace std;

/*
   1. malloc(Memory Allocation)
   -- Purpose: Allocates size bytes of uninitialized memory.
   -- Returns: A pointer to the allocated memory or NULL if allocation fails.

   2. calloc(Contiguous Allocation)
   -- Purpose: Allocates memory for num elements of size bytes each, initializes all to 0.
   -- Returns: Pointer or NULL.

   3. realloc(Re-allocation)
   -- Purpose: Changes the size of previously allocated memory.
   -- Returns: Pointer to the new memory (may be same or different), NULL if fails.
   -- relloc may move the memory block; old pointer may become invalid.
   -- If realloc fails, it returns NULL but original memory is not freed, so you must handle it carefully.
*/

int main(){
    return 0;
}